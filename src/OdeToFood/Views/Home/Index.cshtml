@model OdeToFood.ViewModels.HomePageViewModel

    @*//LD we set the title that "_Layout will render"*@
    @{
        ViewBag.Title = "Home";
    }

    <h1>Welcome!</h1>

    @foreach (var restaurant in Model.Restaurants)
    {
        @Html.Partial("_Summary", restaurant)
    }

    <div>
        <a asp-action="Create">Create</a>
    </div>

  @section footer{
      teste minchia
  }